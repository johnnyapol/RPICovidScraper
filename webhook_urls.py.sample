# webhook_urls.py
# Rename this file to webhook_urls.py (it is .gitignored to avoid conflicts with running instances)
# Update the `WEBHOOKS` list to your desired configuration, along with PSA and QUIET

webhooks = ['my_webhook_url_1', 'my_webhook_url_2', '....', 'my_webhook_url_N']
PSA = None # Displays an additional message, useful for announcements
QUIET = False # Controls 'quiet' mode -- where the bot only posts on new cases
